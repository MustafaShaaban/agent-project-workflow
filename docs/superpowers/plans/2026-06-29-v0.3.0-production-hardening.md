# v0.3.0 Production Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make v0.3.0 safe to install globally and use as a project workflow initializer without overwriting owner-managed content.

**Architecture:** Keep the PowerShell CLI as the single command surface. Harden its managed-block contract with immutable marker constants and exact marker-pair validation, exercise the contract through filesystem-level tests, and make repository formatting and CI policy executable. Keep third-party guard installation owner-approved even though verified install commands are documented.

**Tech Stack:** PowerShell 5.1, Git, GitHub Actions, Markdown, YAML, JSON.

---

### Task 1: Managed-block regression coverage

**Files:**
- Modify: `scripts/test-workflow.ps1`
- Modify: `scripts/project-workflow.ps1`

- [ ] Add tests for immutable non-empty marker constants, exact generated marker pairs, unmanaged-file preservation with a marked suggestion, two-sided owner-content preservation during upgrade, and doctor handling of an incomplete marker pair.
- [ ] Run `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\test-workflow.ps1` and confirm the new immutable-constant and incomplete-pair checks fail.
- [ ] Replace mutable marker variables with script-scoped constants and centralize exact managed-block validation.
- [ ] Run the self-test and confirm the managed-block checks pass.

### Task 2: Formatting and self-CI

**Files:**
- Create: `.gitattributes`
- Create: `.github/workflows/verify.yml`
- Modify: `scripts/test-workflow.ps1`

- [ ] Add recursive anti-collapse checks for repository docs, presets, and PowerShell scripts plus a `.gitattributes` existence check.
- [ ] Add explicit line-ending attributes for Markdown, YAML, JSON, and PowerShell.
- [ ] Add a GitHub Actions workflow that runs the self-test and `git diff --check` on Windows.
- [ ] Renormalize tracked text and confirm the anti-collapse checks pass.

### Task 3: Verified skill policy and release docs

**Files:**
- Modify: `scripts/project-workflow.ps1`
- Modify: `scripts/generate-presets.ps1`
- Modify: `templates/.ai-skills.json`
- Modify: `presets/*/.ai-skills.json`
- Modify: `README.md`
- Modify: `docs/cli.md`
- Modify: `docs/testing.md`
- Modify: `docs/automatic-activation.md`
- Modify: `docs/skills-policy.md`
- Modify: `PROGRESS.md`
- Modify: `CHANGELOG.md`

- [ ] Record the verified `amElnagdy/guard-skills` commands for all five companion guards with `install_approved: false`.
- [ ] Document that `install-skills -ApprovedOnly` does not install those guards until an owner approves them in project policy.
- [ ] Align command, marker, test, CI, and release documentation with observed behavior.

### Task 4: Full verification and publication

**Files:**
- Verify all changed files.

- [ ] Run the full self-test.
- [ ] Run `git diff --check`.
- [ ] Run dry-run, apply, and JSON doctor smoke commands in an isolated repository copy.
- [ ] Review `git diff`, confirm no owner files were lost, commit to `master`, push `master`, and verify `master` is clean and aligned with `origin/master`.
