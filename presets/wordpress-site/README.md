# WordPress Site Preset

Use for whole-site WordPress repositories.

Prioritizes site-level docs, Spec Kit for production changes, `wp-guard`, and keeping business logic out of themes unless the project explicitly chooses theme-only presentation work.
