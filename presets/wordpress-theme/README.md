# WordPress Theme Preset

Use for standalone WordPress themes.

Theme code should stay presentation-focused. Durable business behavior belongs in plugin mode unless the owner explicitly chooses theme-only presentation logic.
