# WordPress Preset

Base WordPress preset for sites, plugins, themes, blocks, and WooCommerce projects.

Requires `wp-guard`, protects core/uploads/cache/vendor/build output, and requires WordPress API, sanitization, escaping, nonce, capability, prepared-query, translation, and conditional asset-loading rules.
