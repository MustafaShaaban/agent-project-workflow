# Generic Preset

Use for projects that do not need framework-specific rules.

Includes generic constitution rules, standard startup, managed blocks, progress/decision logs, and optional Spec Kit setup.
